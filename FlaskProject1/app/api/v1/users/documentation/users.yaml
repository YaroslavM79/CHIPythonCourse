Return all users
---
get:
  description: Return all users
  security:
    - ApiKeyAuth: []
  responses:
    200:
      description: Return a list of all users
      content:
        application/json:
        schema: UserSchema