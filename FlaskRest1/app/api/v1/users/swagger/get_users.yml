tags:
  - users
summary: Get list of users
security:
  - basicAuth: [ ]
responses:
  200:
    description: A list of users
    schema:
      type: array
      items:
        $ref: '#/definitions/User'
  401:
    $ref: '#/definitions/Error401'
  404:
    $ref: '#/definitions/UserError404'
